import React from "react";
import BannerSVG from "@/assets/branding/NeonLaunch_banner.png"; 
// ?url sorgt dafür, dass Vite die Datei als URL liefert

export const Navbar: React.FC = () => (
  <header className="nav">
    <div className="nav__left">
      <a className="brand" href="#top" aria-label="Start">
        <img
          src={BannerSVG}
          alt="NeonLaunch"
          className="brand__logo"
          height={40}
          decoding="async"
          loading="eager"
          fetchPriority="high"
        />
       
      </a>

      <nav className="menu" aria-label="Navigation">
        <a href="#create">Create</a>
        <a href="#how">How it works</a>
        <a href="#explore">Explore</a>
        <a href="#faq">FAQ</a>
      </nav>
    </div>
  </header>
);